using System;

// Model

namespace LesApp0
{    
    class Model
    {
        public string Logic(string s)
        {
            return string.Format("Работа: Model.Logic({0})", s);
        }
    }
}
